//DJRG: Cory, I noticed this script was throwing compile errors. You might need to rewrite it a bit. 

//var scrollspeed = 0.25;
//function FixedUpdate ()
//{
//var offset = Time.time *scrollspeed;
//render.material Textureoffset = Vector z (offset.ofset);
//
//}