using UnityEngine;
using System.Collections;

public class GuiForMainScreen : MonoBehaviour {
	public GUIStyle mystyle;
	void OnGUI(){
	//	GUI.Button (new Rect (10,10,500,25), "Press down left button of mouse to rotate objects");
	//	GUI.Button (new Rect (10,40,500,25), "'up/down/left/right' or 'w/s/a/d'for walking, 'P' for Pick up/Put back objects");
	//	GUI.Button (new Rect (10,70,500,25), "'F' for fix/unfix screen, 'T' for put objects into cart");
	//	GUI.Button (new Rect (10,100,500,25), "'C' for comparision (use after 'P'), 'esc' to move mouse to quit program");
		
	}
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
